package com.cnc.demo.repository;

import com.cnc.demo.model.CuttingMode;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CuttingModeRepository extends JpaRepository<CuttingMode, Long> {
}
