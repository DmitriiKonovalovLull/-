package com.cnc.demo.repository;

import com.cnc.demo.model.SetDiametr;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SetDiametrRepository extends JpaRepository<SetDiametr, Long> {
}
